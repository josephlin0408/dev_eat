<%= render 'superadmin/share/menu' %>
<div class="container">
	<div class="row">
		Product Nav =>
		<%= link_to 'Back Menu', new_superadmin_store_category_path %>
	</div>
</div>	
<hr>
<div class="container">
	<div class="row">
		<%= bootstrap_form_for [:superadmin, @store_categories] do |f| %>
		<%= f.hidden_field :store_id, value: session[:sid] %>
		<%= f.text_field :title %>
		<%= f.text_field :anchor %>
		<%= f.submit 'submit' %>
		<% end %>
	</div>
</div>