<%= render 'superadmin/share/menu' %>
<div class="container">
	<div class="row">
		Nav =>
		<%= link_to 'Back Store', superadmin_store_path(session[:sid]) %><br>
		Action =>
		<%= link_to '新增分類', new_superadmin_store_category_path %> | 
	</div>
</div>	
<hr>
<div class="container">
	<div class="row">
		<% @store_categories.each do |f| %>
		<%= f.title %>
		<% end %>
	</div>
</div>