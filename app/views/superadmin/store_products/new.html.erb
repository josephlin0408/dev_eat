<%= render 'superadmin/share/menu' %>
<div class="container">
	<div class="row">
		Nav =>
		<%= link_to 'Back List', superadmin_store_products_path %>
	</div>
</div>	
<hr>
<div class="container">
	<div class="row">
		<%= bootstrap_form_for [:superadmin, @store_product] do |f| %>
		<%= f.hidden_field :store_id, value: session[:sid] %>
		<%= f.text_field :name %>
		<%= f.select :category, @store_category.map { |c| [c.title]} %>
		<%= f.text_field :price %>
		<%= f.text_area :desc %>
		<%= f.submit 'submit' %>
		<% end %>
	</div>
</div>
<%= now_add %>